# ==================================================
# DWG to JPG Converter API 配置文件示例
# 复制此文件并重命名为 .env，然后修改为您的实际配置
# ==================================================

# --------------------------------------------------
# 数据库连接配置
# --------------------------------------------------
# SQL Server数据库服务器地址
DB_SERVER=localhost

# SQL Server数据库名称
DB_DATABASE=your_database

# 数据库用户名
DB_USERNAME=your_username

# 数据库密码
DB_PASSWORD=your_password

# 数据库驱动（通常不需要修改）
DB_DRIVER={ODBC Driver 17 for SQL Server}

# --------------------------------------------------
# 应用基础配置
# --------------------------------------------------
# 应用监听主机地址
APP_HOST=0.0.0.0

# 应用监听端口号
APP_PORT=8000

# 是否启用热重载（开发环境设置为true，生产环境设置为false）
RELOAD=true

# 临时文件目录
TEMP_DIR=./temp

# --------------------------------------------------
# 文件路径配置
# --------------------------------------------------
# DWG文件路径前缀（用于相对路径拼接）
# 当数据库中存储的是相对路径时，会与这个前缀拼接成完整路径
DWG_FILE_PREFIX=D:\Data

# --------------------------------------------------
# 日志配置
# --------------------------------------------------
# 日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL）
LOG_LEVEL=INFO

# 日志文件路径（留空则只输出到控制台）
LOG_FILE=logs

# --------------------------------------------------
# 定期任务配置
# --------------------------------------------------
# 检查间隔（单位：秒）
# 定义API多久检查一次数据库中的DWG文件是否需要转换
CHECK_INTERVAL=300  # 5分钟执行一次